/**
 * Generic CPU backend — placeholder
 *
 * The reference implementations live in src/core/ (tq_polar.c, tq_qjl.c, etc).
 * This file is reserved for optimized generic (non-SIMD) CPU kernels.
 */

#include "turboquant/turboquant.h"

/* Generic CPU kernels will be added here as needed.
   For now, all computation uses the ref implementations in src/core/. */
